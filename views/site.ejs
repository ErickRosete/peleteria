<%- include(header,{my_title}) %>
    <% if(cssFile!="") { %>
        <%-include(cssFile)%>
            <% } %>

                <body class="custom_body">
                    <%-include(navbar)%>
                        <%-include(content)%>
                            <%-include(footer) %>
                                <% if (scriptFile!="") { %>
                                    <%-include(scriptFile) %>
                                        <% } %>
                </body>

                </html>